# Example Hello pack

This harmless pack validates the official registry lifecycle without adding a
real specialist domain to the default agent007 catalog. It contains one inert
skill, requests no network access, and performs no external actions.

The pack is not installed by `agent007 init`. It becomes visible only after an
explicit pack installation and disappears from catalog lookup when disabled.
